server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tag?serverTimezone=UTC
    username: root
    password: Liwenlong.0328
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  global-config:
    db-config:
      table-prefix: "tbl_"
      id-type: auto

#      logic-delete-field: delete  # 全局逻辑删除的实体字段名(since 3.3.0,配置后可以忽略不配置步骤2)
#      logic-delete-value: 0 # 逻辑已删除值(默认为 1)
#      logic-not-delete-value: 1 # 逻辑未删除值(默认为 0)

